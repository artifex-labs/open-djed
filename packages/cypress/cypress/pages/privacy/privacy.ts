import Elements from './privacyElements';

export default class Privacy extends Elements {
	readonly url: string = '/privacy'; // URL of the page
	readonly fixturesPath: string = 'privacy'; // Fixtures subfolder where the translations for this object is stored
}